{
  "installCommand": "yarn install",
  "build": {
    "env": {
      "PYTHON_VERSION": "3.12"
    },
    "commands": {
      "preinstall": "apt-get update && apt-get install -y python3-distutils"
    }
  }
}
